import 'package:flutter/cupertino.dart';

class  CreateFileDevelop extends StatelessWidget {
  const CreateFileDevelop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: const [
          Text('Developer 1 create file 1')
        ],
      ),
    );
  }
}
